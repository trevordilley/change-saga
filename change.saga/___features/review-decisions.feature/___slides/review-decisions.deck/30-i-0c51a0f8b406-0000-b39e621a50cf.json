{
  "version": 4,
  "id": "authorize",
  "slide": "decision-write-path",
  "rank": 0,
  "kind": "transition",
  "label": "Authorize mutation",
  "description": "The HTTP server bounds and parses the form, validates the process token, rejects frozen reviews, and resolves the current head. The enhanced browser also submits its shown-slide snapshot.",
  "selector": {
    "type": "element",
    "element_id": "authorize"
  }
}
